import ScreenStub from "@/components/ScreenStub";

export default function AppointmentsPage() {
  return (
    <ScreenStub
      status="Stub — pending interviews"
      title="Appointments & cost tracker"
      painPoint="Tracks antenatal contacts against WHO's 8-contact model, and logs costs as they're incurred — directly addressing the Nigeria-specific pain point of unexpected charges at first registration and beyond."
    />
  );
}